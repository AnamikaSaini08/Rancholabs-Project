export interface IStar {
  position: [x: number, y: number, z: number];
  // deleteCoorBattery: any[];
  // isStarToDelete: any;
  // resetFlag: boolean;
  // setResetFlag: React.Dispatch<React.SetStateAction<boolean>>;
}

export interface IBox {
  position: [x: number, y: number, z: number];
}
